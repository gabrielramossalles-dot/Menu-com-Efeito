// Garante que o hover seja detectado de forma limpa
document.querySelectorAll('.menu-item').forEach(item => {
    item.addEventListener('mouseenter', () => {
        // Você pode adicionar sons de "click" aqui se quiser estilo editor!
    });
});